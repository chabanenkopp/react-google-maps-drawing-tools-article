import styled from "styled-components";
import { Color } from "Theme";
import { Box } from "components/Layout";

export const CardDivider = styled(Box)`
  width: 100%;
  height: 1px;

  background-color: ${Color.BleachedSilk};
`;
